---
import { Image } from 'astro:assets';
// ¡¡¡ASEGÚRATE DE QUE EL NOMBRE Y EXTENSIÓN DE LA FOTO DE TU PAPÁ SEAN CORRECTOS!!!
import dadPhoto from '../assets/dad-photo.jpeg';
---
<section id="content-block-section">
    <div class="content-wrapper">
        <div class="text-column">
            <h2>Tu experto local de confianza.</h2>
            <p>Hola, soy <strong>Abel Diaz</strong>. Mi enfoque es simple: ofrecerte la claridad y el respaldo que necesitas para tomar las mejores decisiones. Como tu vecino en Aguascalientes y Guadalajara, estoy aquí para proteger lo que más valoras.</p>
            <a href="/contacto" class="content-button">Hablemos de tus metas</a>
        </div>
        <div class="image-column">
            <div class="image-container">
                <Image src={dadPhoto} alt={"Foto profesional de " + "[Nombre de tu Papá]"} class="profile-image"/>
            </div>
        </div>
    </div>
</section>

<style>
    #content-block-section {
        /* ¡El bloque azul! */
        background-color: var(--azul-vibrante);
        color: var(--blanco-puro);
        padding: 8rem 2rem;
    }
    .content-wrapper {
        display: grid;
        grid-template-columns: repeat(2, 1fr);
        align-items: center;
        gap: 4rem;
        max-width: 1200px;
        margin: 0 auto;
    }
    .text-column h2 {
        font-size: 3.5rem;
        font-weight: 900;
        line-height: 1.2;
        margin-bottom: 2rem;
    }
    .text-column p {
        font-size: 1.25rem;
        line-height: 1.8;
        margin-bottom: 2.5rem;
        opacity: 0.9;
    }
    .content-button {
        display: inline-block;
        font-weight: 700;
        font-size: 1.1rem;
        color: var(--gris-oscuro);
        background-color: var(--dorado-acento); /* ¡El toque dorado! */
        padding: 1rem 2.5rem;
        text-decoration: none;
        border-radius: 8px;
        transition: transform 0.2s ease;
    }
    .content-button:hover {
        transform: scale(1.05);
    }
    .image-container {
        border-radius: 12px;
        overflow: hidden;
        border: 4px solid var(--blanco-puro);
        box-shadow: 0 15px 30px rgba(0,0,0,0.2);
    }
    .profile-image {
        width: 100%;
        height: auto;
        display: block; /* Elimina espacio extra debajo de la imagen */
    }

    @media (max-width: 900px) {
        .content-wrapper {
            grid-template-columns: 1fr;
        }
        .image-column {
            grid-row: 1; /* Pone la imagen primero en móvil */
            margin-bottom: 3rem;
        }
        .text-column h2 {
            font-size: 2.5rem;
        }
    }
</style>