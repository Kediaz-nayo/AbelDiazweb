---
// src/components/Hero.astro
---
<section id="hero-section">
    <div class="hero-content">
		<h1 data-aos="fade-up">Insurance that makes sense.</h1>
		<p data-aos="fade-up" data-aos-delay="200">Finally, straightforward advice from a local expert you can trust.</p>
    </div>
</section>

<style>
    #hero-section {
        /* This is the key: a full-height, colored block */
        min-height: 90vh;
        width: 100%;
        background-color: var(--accent); /* Our bold color */
        color: var(--white);
        
        display: flex;
        flex-direction: column;
        justify-content: flex-end; /* Aligns content to the bottom */
        padding: 4rem 2rem;
    }
    .hero-content {
        max-width: 1200px;
        width: 100%;
        margin: 0 auto;
    }
    h1 {
        font-size: 6rem; /* HUGE text */
        font-weight: 700;
        line-height: 1;
        margin-bottom: 2rem;
    }
    p {
        font-size: 1.5rem;
        font-weight: 500;
        max-width: 600px; /* Constrain the paragraph width */
    }

    @media (max-width: 768px) {
        h1 { font-size: 3.5rem; }
        p { font-size: 1.25rem; }
    }
</style>