---
import Layout from '../layouts/Layout.astro';
---

<Layout 
    title="Contacto - Asesoria patrimonial y seguros" 
    description="Contáctame para una cotización de seguro sin costo y sin compromiso."
>
    <header id="contact-header">
        <a href="/" class="back-link">← Volver al Inicio</a>
    </header>

    <main id="contact-main">
        <div class="contact-wrapper">
            <div class="text-content">
                <h1>Hablemos.</h1>
                <p>
                    Estoy aquí para responder tus preguntas y ayudarte a encontrar la mejor cobertura. 
                    Envíame un mensaje y me pondré en contacto contigo a la brevedad.
                </p>
                <div class="info-details">
                    <p>
                        <strong>Email:</strong> <a href="mailto:admonproy04@gmail.com">admonproy04@gmail.com</a>
                    </p>
                    <p>
                        <strong>Teléfono:</strong> <a href="tel:+52[449 547 8201]">+52 449 547 8201</a>
                    </p>
                </div>
            </div>

            <form name="contact" method="POST" data-netlify="true" class="contact-form">
                <input type="hidden" name="form-name" value="contact" />
                
                <div class="form-group">
                    <label for="name">Tu Nombre</label>
                    <input type="text" name="name" id="name" placeholder="Juan Pérez" required />
                </div>

                <div class="form-group">
                    <label for="email">Tu Correo Electrónico</label>
                    <input type="email" name="email" id="email" placeholder="juan.perez@email.com" required />
                </div>

                <div class="form-group">
                    <label for="message">Mensaje</label>
                    <textarea name="message" id="message" rows="5" placeholder="Hola, me gustaría cotizar un seguro de..." required></textarea>
                </div>
                
                <button type="submit" class="submit-button">Enviar Mensaje</button>
            </form>
        </div>
    </main>
</Layout>

<style>
    /* Estilo del Encabezado simple */
    #contact-header {
        padding: 2rem;
    }
    .back-link {
        font-weight: 700;
        color: var(--gris-oscuro);
        text-decoration: none;
        font-size: 1.1rem;
    }

    /* Contenido Principal */
    #contact-main {
        display: flex;
        align-items: center;
        padding: 2rem 2rem 6rem 2rem;
    }
    .contact-wrapper {
        max-width: 1100px;
        margin: 0 auto;
        display: grid;
        grid-template-columns: 1fr 1.2fr; /* Columna de texto un poco más pequeña */
        gap: 5rem;
        align-items: center;
    }

    /* Columna de Texto */
    .text-content h1 {
        font-size: 5rem;
        font-weight: 900;
        line-height: 1;
        color: var(--azul-vibrante);
        margin-bottom: 2rem;
    }
    .text-content p {
        font-size: 1.25rem;
        line-height: 1.6;
    }
    .info-details {
        margin-top: 3rem;
        font-size: 1.1rem;
    }
    .info-details p {
        margin-bottom: 1rem;
    }
    .info-details a {
        color: var(--gris-oscuro);
        text-decoration: none;
        font-weight: 400;
    }
    .info-details a:hover {
        text-decoration: underline;
    }

    /* Formulario */
    .form-group {
        margin-bottom: 1.5rem;
    }
    .form-group label {
        display: block;
        font-weight: 700;
        margin-bottom: 0.75rem;
    }
    .form-group input,
    .form-group textarea {
        width: 100%;
        padding: 1rem;
        font-family: 'Poppins', sans-serif;
        font-size: 1rem;
        border: 2px solid #e5e7eb;
        border-radius: 8px;
        transition: border-color 0.2s ease;
    }
    .form-group input:focus,
    .form-group textarea:focus {
        outline: none;
        border-color: var(--azul-vibrante);
    }
    .submit-button {
        width: 100%;
        padding: 1.2rem;
        font-size: 1.1rem;
        font-weight: 700;
        color: var(--gris-oscuro);
        background-color: var(--dorado-acento); /* ¡El color dorado! */
        border: none;
        border-radius: 8px;
        cursor: pointer;
        transition: transform 0.2s ease;
    }
    .submit-button:hover {
        transform: scale(1.02);
    }

    @media (max-width: 900px) {
        .contact-wrapper {
            /* Cambiamos el grid de 2 columnas a 1 sola */
            grid-template-columns: 1fr;
        }
        .text-content {
            text-align: center;
            margin-bottom: 3rem;
        }
        .text-content h1 {
            font-size: 3.5rem;
        }
    }
</style>